import { tserrArktypePlugin } from './tserr-arktype-plugin';

describe('tserrArktypePlugin', () => {
  it('should work', () => {
    expect(tserrArktypePlugin()).toEqual('tserr-arktype-plugin');
  });
});
